version: "3"
services:
  lprnet-tao-toolkit:
    container_name: lprnet-tao-toolkit
    build:
      context: .
      dockerfile: ./Dockerfile
    volumes:
      - ./:/app/src
    working_dir: /app/src
    tty: true
